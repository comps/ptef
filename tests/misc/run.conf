+ parallel_jobs
+ parallel_jobs_extrajobs
+ open_fds
+ run_before_pass
+ ignored
+ exit_codes
case "$TEST_VARIANT" in	cli|valgrind|python)
	+ runner_color_map
	;;
esac
case "$TEST_VARIANT" in	cli|valgrind)
	+ exit_codes_sanity
	+ cli_help
	+ cli_noargs
	;;
esac

# vim: syntax=sh
