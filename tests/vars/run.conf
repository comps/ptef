# Other variables (PTEF_PREFIX / PTEF_LOGS / ..) are tested elsewhere

+ ptef_run
+ ptef_run_opt
+ ptef_results_fd
+ ptef_results_fd_nocolor
+ ptef_results_fd_closed
+ ptef_silent
+ ptef_silent_opt
+ ptef_nologs
+ ptef_nologs_opt
# older util-linux doesn't have realpath
if command -v realpath >/dev/null; then
	+ ptef_shell
fi
+ ptef_shell_opt
+ ptef_color

# vim: syntax=sh
