function ptef_git_version {
	local version=$(git describe --long --tags HEAD)  # v0.7-113-g1a9f9a3
	version="${version#v}"     # 0.7-113-g1a9f9a3
	version="${version//-/.}"  # 0.7.113.g1a9f9a3
	echo -n "$version"
}

# vim:syntax=sh
